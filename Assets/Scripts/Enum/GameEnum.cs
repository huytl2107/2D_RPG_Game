public class GameEnum
{
    public enum EPlayerState{idle, walk, attack, farming, fishing};
    public enum EPlayerFacing{front, horizontal, back};
    public enum EPlantState{nonGrown1, nonGrown2, nonGrown3, nonGrown4, grown};
}
